# Docker Environment Configuration
# Kopiere diese Datei zu .env.docker.local und fülle die Werte aus

# Database
DB_PASSWORD=changeme123
DATABASE_URL=postgresql://dartsturnier:changeme123@postgres:5432/dartsturnier

# Redis
REDIS_PASSWORD=changeme123
REDIS_URL=redis://:changeme123@redis:6379

# NextAuth (WICHTIG: Ändere NEXTAUTH_SECRET!)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=please-change-this-to-a-random-32-char-string-min-32-chars

# SMTP (Optional - für Magic Link Authentication)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@dartsturnier.local

# Stripe (Optional - für Zahlungen)
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=

# App Configuration
NODE_ENV=production
PORT=3000
