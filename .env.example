# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT CONFIGURATION
# Rename this file to .env before starting docker-compose
# -----------------------------------------------------------------------------

# --- SECURITY SECRETS (CHANGE THESE!) ---
# Generieren Sie sichere Passwörter!
DB_PASSWORD=changeme123_secure_random_string
REDIS_PASSWORD=changeme123_secure_random_string

# Generieren mit: openssl rand -base64 32
NEXTAUTH_SECRET=your_generated_secret_here

# Generieren mit: openssl rand -hex 32
DATA_ENCRYPTION_KEY=your_generated_encryption_key_here

# --- APP CONFIGURATION ---
# WICHTIG: Hier muss Ihre ECHTE Domain oder öffentliche IP stehen!
# NICHT localhost oder 0.0.0.0 verwenden, sonst funktionieren die E-Mail-Links nicht.
# Beispiel: https://darts.mein-verein.de oder http://192.168.178.50:3000
NEXTAUTH_URL=http://localhost:3000
NODE_ENV=production

# --- SMTP EMAIL CONFIGURATION ---
SMTP_HOST=your_smtp_host
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email
SMTP_PASS=your_smtp_password
SMTP_FROM="Darts Turnier" <your_email>

# --- BRANDING ---
SMTP_LOGO_URL=your_logo_url
BRAND_NAME="Darts Masters"
BRAND_COLOR=#dc2626

# --- STRIPE (Optional) ---
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# --- LOCAL ACCESS (For running scripts from host machine) ---
# Diese URLs werden von Prisma auf dem Host verwendet.
# Im Docker-Container werden sie automatisch überschrieben.
DATABASE_URL="postgresql://dartsturnier:${DB_PASSWORD}@localhost:5432/dartsturnier"
REDIS_URL="redis://:${REDIS_PASSWORD}@localhost:6379"
